CONSTANTS
    (* Set constants specific to algo *)
    Nil <- TraceNil
    NoVal <- TraceNil
    Key <- TraceKey
    Val <- TraceVal
    TxId <- TraceTxId
    (* Set default action for TVOperators module *)
    Default <- DefaultImplementation
    (* Set variables of TraceSpec module *)
    Vars <- vars
    (* Set operator for TraceSpec module *)
    BaseInit <- Init
    UpdateVariables <- UpdateVariablesImplementation
    TraceNext <- TraceNextImplementation

(* NOTHING TO MODIFY BELOW *)
SPECIFICATION
    TraceSpec

VIEW
    TraceView

PROPERTIES
    BaseSpec

POSTCONDITION
    TraceAccepted

CHECK_DEADLOCK
    FALSE